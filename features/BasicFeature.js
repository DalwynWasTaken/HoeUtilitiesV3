import { UIContainer } from "../../Elementa";

// IS BEING WORKED ON

//! Main issue is that this won't allow them to be all alligned under 
//! eachother without a bit of playing around

export class DisplayText {
    constructor(text) {
        this.container = new UIContainer()
            .setX()
    }

    setKeyText() { }

    setValueText() { }

    setColor() { }

    setChildOf(hud) {
    }
} 